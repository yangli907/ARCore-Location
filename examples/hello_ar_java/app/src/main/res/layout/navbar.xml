<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <FrameLayout
        android:id="@+id/stops_list_holder"
        android:layout_width="200dp"
        android:layout_height="450dp"
        android:background="#a3a7a9"
        android:alpha="0.7">
        <TableLayout
            android:id="@+id/stops_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingLeft="20dp"
            android:paddingTop="60dp"></TableLayout>
    </FrameLayout>

    <Button
        android:id="@+id/toggleDisplay"
        android:layout_width="160dp"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:text="Toggle Display" />

    <RelativeLayout
        android:id="@+id/mapView_holder"
        android:layout_width="160dp"
        android:layout_height="120dp"
        android:background="#a3a7a9"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:alpha="0.7">
        <ImageView
            android:id="@+id/mapView"
            android:layout_width="80dp"
            android:layout_height="60dp"
            android:layout_alignParentBottom="true"
            android:layout_alignParentRight="true"
            android:paddingBottom="10dp"
            android:paddingRight="10dp"
            android:alpha=".4"
            android:src="@drawable/bluepin"
            />
    </RelativeLayout>

</RelativeLayout>